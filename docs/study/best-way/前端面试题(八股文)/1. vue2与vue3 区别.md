# vue2 与 vue3 区别

1. vue3 默认使用 composition api，vue2 默认使用 options api
2.

## 开发体验上

1. 新增了 hooks, 函数式组件风格
2. v-model modelValue
3. options api 改为 composition api setup 语法
   1. watchEffect
   2. ref reactive
   3.
4.

## 底层优化

1. 模板编译-预字符串化
2. 函数缓存
3. 静态节点标记 dom
4. dom diff
5. proxy 替代 object.defineProperty
   1. 更好的性能, 解决了一些动态数据添加不不支持响应式的问题. 但是也新增了解构不支持响应式的问题
6. ts 更好的类型支持
7.

##
